class A {
	private static int x, y;
	public static Clase x;

	static void main() {
		A.f();

		A.f = B.g;

		int x = B.g / -C.d + 1;

		A.f().g = 3;
		
		new A().f(B.g());

		return B.g(3, f() + 5).x;		
	}
}