interface A<> {
	dynamic Clase<> k(BigInteger<K> c, boolean x);
}

interface B extends A< {}

interface C extends A, B {
	static void f();	
}

class D<K> implements A> {
	private static Clase<> k;
}

class E<K> extends D<K, d> {
	E<K>() {

	}

	static int main() {
		Clase< c = new Clase();
		Clase<K> c = new Clase<extends>);

		new Clase<>().x;

		int x = Clase<K>.x();
	}
}