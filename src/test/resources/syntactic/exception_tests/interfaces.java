interface a {
	static ;
	static int f()
	static String (BigInteger x);
	dynamic Clase k;
	int f(int a, char c);
}

interface B implements A {}

interface A

interface C extends {
	static void f();	
}

class D implements {}

class E extends D implements k {}