class A {
	private static x, y;
	public static Clase;

	static void main() {
		A f;
		int x = A f;

		A.f. = B.g;

		int x = B.c / -C.D;

		A.f().public = 3;
		
		new A().f(B));

		return B.g(,).x;
		return B.g(a,).x;		
	}
}